# 📋 CHANGELOG — موشک موزیک (اندروید)

## v6.0.2 🔥 (نسخهٔ فعلی)
**فیکس‌های گزارش کاربر:**
- 🎵 **TRACKS بعد از «افزودن به کتابخانه» خالی بود** — ریشه: نسخهٔ ۶.۰.۱ موقع انتقال نتایج اسکن به کتابخانه اشتباهاً تراک‌هایی که در کتابخانه بودند را کلاً رد می‌کرد؛ `mergeNew` بازنویسی شد (فقط مقصد دیتاچک می‌شود)
- ⚡ **اندروید خیلی کند شده بود** — `mergeNew` و دیتاچک اسکن O(n²) روی CopyOnWriteArrayList بودند؛ حالا با HashSet (O(n)) — به‌مراتب سریع‌تر روی هزاران موزیک
- 🔕 چک دوره‌ای دنبال‌شده‌ها سبک‌تر شد (۳۰ دقیقه به‌جای ۱۵ + شروع دیرتر) — دیپ‌اسکن پس‌زمینه دیگر اپ را قفل نمی‌کند
- 🚀 **لوگوی لانچر جدید** (موشک + نت موسیقی) در همهٔ سایزها
- 🖥 **ویندوز**: دکمه‌های پلیر فول‌اسکرین کار نمی‌کردند (کانواس ویژوالایزر روی دکمه‌ها افتاده بود — `pointer-events:none`)
- 🖥 **ویندوز**: فولدرهای تلگرام واقعی (getChatFolder — همه/آرشیو/سفارشی)
- 🖥 **ویندوز**: دیپ‌اسکن کامل چت‌ها فیکس شد (گارد اسکن هم‌زمان + ریست پرچم لغو)
- 🖥 **ویندوز**: «اسکن دوتایی» رفع شد (چک پس‌زمینهٔ دنبال‌شده‌ها دیگر روی صفحهٔ اسکن پراگرس نشان نمی‌دهد)
- 🖥 **ویندوز**: ویس‌ها دیگر در آهنگ‌ها نیستند (فقط messageAudio و فایل صوتی)
- 🖥 **ویندوز**: فیکس باگ ایندکس ردیف‌ها در چت‌ها/کانال‌ها (با data-cid)

## v6.0.1
**فیکس بحرانی گزارش کاربر:**
- 🐛 **TRACKS خالی بود حتی بعد از «افزودن به کتابخانه»**: اسکن دستی اشتباهاً مستقیم به کتابخانه می‌رفت (پارامتر `toLibrary=true`) و دکمهٔ افزودن کاری نمی‌کرد — حالا نتایج اول به بخش SCAN می‌روند و با دکمه واقعاً به TRACKS منتقل می‌شوند
- 🔁 باگ `mergeNew`: همیشه روی `library` دیتاچک می‌کرد نه روی مقصد واقعی — اصلاح شد
- 🔔 هوک‌های زندهٔ کتابخانه چندتایی شدند — فرگمنت FOLLOWED دیگر هوک رفرش TRACKS را بازنویسی نمی‌کند (تب آهنگ‌ها بعد از اسکن/فالو زنده به‌روز می‌شود)

## v6.0.0
- 🖥 نسخهٔ ویندوز (Electron) بازسازی کامل: بخش دنبال‌شده‌ها 🔔، اسکن همهٔ چت‌ها یک‌جا، فولدرهای تلگرام (همه/آرشیو/سفارشی)، لوگوی داخل اپ فیکس شد
- 📚 «افزودن به کتابخانه» مسیر درست، لود async کتابخانه (ضد ANR)
- 🍎 نسخهٔ iOS هم به ۶.۰.۰ رسید

## v5.9.5
**فیکس‌های بحرانی:**
- 💥 کرش ProxyFragment (ping از thread → requireContext null) — کانتکست app-level
- 🔒 «اسکن در حال انجامه» دروغ: چک فالو‌ها پرچم جدا گرفت (`followChecking`) و دیگر اسکن دستی را قفل نمی‌کند
- 📚 **کتابخانه دائمی شد** (`library.json`): هر «افزودن به کتابخانه»/فالو-دیپ‌اسکن/آهنگ جدید فالو ذخیره می‌شود — بعد از ری‌استارت TRACKS و FAVORITES دیگر خالی نیستند
- 📜 صفحه دنبال‌شده‌ها داخل NestedScrollView — اسکرول کامل

## v5.9.4
- 🐛 فیکس مسیریابی: نتایج فالو→کتابخانه (TRACKS)، فقط آهنگ‌های جدید در FOLLOWED
- 📌 نوتیف اسکن پین‌شده (PRIORITY_MAX + ongoing)

## v5.9.3
- 💥 ضدکرش کامل bindSideMenu (NPE لاگ کاربر)
- 💜 ویژوالایزر نئونی بازنویسی از صفر (`NeonVisualizer`)
- 🔔 کارت‌های امن دنبال‌شده‌ها (حذف فقط با تأیید)
- 🔍 حذف دکمه سرچ از هدر + 📝 فیکس نسخه تنظیمات
- 🎁 دیالوگ «افزودن به کتابخانه؟» بعد از اسکن
- 🗑 دکمه «پاک کردن کتابخانه» در تنظیمات

## v5.9.2
- 🐛 وایر شدن واقعی UI انتخاب گروهی (ریشه: پچ ناقص)
- 💔 حذف گروهی از فیوریت

## v5.9.1
- 🔔 تب FOLLOWED کنار TRACKS (تب دوم)
- 📥 فالو = دیپ‌اسکن کامل خودکار
- 💜 فیکس ویژوالایزر (کاور ۶۴٪)
- 🔍 دکمه دیپ‌اسکن کانال‌ها

## v5.9.0
- 🔍 اسکن عمیق بی‌نهایت (بدون سقف ۳۰۰۰)
- ☑️ انتخاب گروهی آهنگ‌ها/دانلودها
- 🔔 فالو کردن چت‌ها/کانال‌ها + صفحه دنبال‌شده‌ها
- 📲 نوتیف اسکن (پیشرفت + پایان)
- 💜 ویژوالایزر نئونی + 💥 پالس کاور

## v5.9.x - v5.8.x (خلاصه)
- v5.8.3: کرش خاتمه نشست از راه دور (gate + توقف امن پخش)
- v5.8.2: کارت کلید API شخصی در تنظیمات (فیکس «کد نمی‌آید»)
- v5.8.1: فیکس کرش استارتاپ (sideProxy)
- v5.8.0: کاور دایره‌ای HD، سپر اتصال، سوییچ پروکسی، سرچ چت‌ها، حذف فایل واقعی

## v5.7.0
- 💾 فقط فیوریت/پلی‌لیست دائمی؛ بقیه با اسکن
- ✏️🗑⬇️ ادیت/حذف/دانلود پلی‌لیست
- 💬 تب چت‌ها + 🇮🇷 «آهنگ‌ها»

## v5.6.0
- 🎨 رنگ اکسنت واقعی (۶ تم کامل)
- 🌐 دوزبانه FA/EN کامل
- 🖼 لوگوی جدید شفاف

## v5.5.0
- 🎨 ری‌دیزاین مطابق اسکرین‌شات‌های استور
- 🖼 تامبنیل‌ها (کاور آلبوم→کانال)
- 🔍 فیکس سرچ + تب SCAN جدا + ویژوالایزر
