# 📋 CHANGELOG — موشک موزیک (اندروید)

## v5.10.9 🎯 (نسخهٔ فعلی)
**فیکس کامل‌بودن اسکن + لوگوی جدید:**
- 🐛 **فیکس «فقط ۵۰ آهنگ»**: اسکن (۵۰/۱۰۰/۳۰۰ چت) دیگر هیچ سقفی روی پیام‌های هر چت ندارد — کل تاریخچهٔ هر چت خوانده می‌شود، پس همهٔ آهنگ‌ها پیدا می‌شوند (قبلاً فقط ۱۵۰ پیام آخر هر چت خوانده می‌شد و در چت‌های پرموزیک ~۵۰ آهنگ بیشتر پیدا نمی‌شد)
- 🐛 **فیکس «۶۰۰ یافت شد ولی ۵۰ نشان داده می‌شود»**: کارت فالو حالا تعداد واقعی آهنگ‌های کتابخانهٔ همان چت را نشان می‌دهد — دقیقاً همان چیزی که با لمس می‌بینید
- ⚡ حذف تأخیر بین چت‌ها (sleep) — اسکن سریع‌تر
- ⚡ سرچ مکمل فقط در حالت «همهٔ چت‌ها» اجرا می‌شود — اسکن سبک‌تر
- 🖼 لوگوی جدید اپ (همان تصویری که ارسال کردید)

## v5.10.8 🚀
**اسکن سریع‌تر + لاگ واضح‌تر:**
- ⚡ اسکن سریع (۵۰/۱۰۰/۳۰۰ چت) فقط ۱۵۰ پیام آخرِ هر چت را می‌خواند — چند برابر سریع‌تر از قبل (قبلاً کل تاریخچهٔ هر چت را می‌خواند)
- ⚡ سرچ سنگین روی چت‌های بزرگ فقط در حالت عمیق اجرا می‌شود، نه در اسکن سریع
- ⚡ صفحات تاریخچه ۵۰ → ۱۰۰ پیام (نصف راندتریپ) + کاهش تأخیر بین چت‌ها (۸۰۰ → ۳۰۰ms)
- 🔍 لاگ زندهٔ اسکن: خط شروع هر چت («در حال اسکن…»)، خط پایان با زمان هر چت، و پیشرفت داخل چت‌های طولانی
- 📋 اسکن عمیق («همهٔ چت‌ها» / دیپ‌اسکن تکی / فالو / Saved) مثل قبل کل تاریخچه را می‌خواند

## v5.10.7 🔥
**فیکس‌های بحرانی:**
- 💥 ضدکرش `ProxyFragment.refreshList`: بعد از ping در ترد پس‌زمینه، اگر فرگمنت جدا شده بود دیگر `requireContext()` صدا زده نمی‌شود (گارد `isAdded`)
- 🔍 **اسکن عادی درست شد**: نتایج اسکن دستی حالا به «بخش اسکن» (scanResults) می‌روند — قبلاً مستقیم به کتابخانه می‌رفتند و صفحهٔ اسکن خالی می‌ماند (فقط دیپ‌اسکن درست بود)
- 🛡 اسکن مقاوم شد: یک چت خراب دیگر کل اسکن را نمی‌کشد (try/catch به‌ازای هر چت) + شمارندهٔ پیام مستقل برای هر چت
- ⭐ `deepScanSaved` پرچم `scanning` را از ابتدا ست می‌کند تا اسکن هم‌زمان راه نیفتد
- 🧹 لاگ گمراه‌کنندهٔ «آهنگ‌های جدید» در دیپ‌اسکن حذف شد

## v5.10.6
- 💜 ویژوالایزر نرم‌تر و پایدارتر (اینترپولاسیون نرم بین فریم‌ها)
- 📥 دانلود در Now Playing و پروکسی — پیشرفت زنده + دکمهٔ لغو
- 🖼 کاور از خود موزیک (`album_cover_thumbnail`) + فال‌بک عکس چت/کانال
- 🖥 پاریتی ویندوز (ویژوالایزر، دانلود، نسخه، کاور)

## v5.10.5
- 💜 بازنویسی ویژوالایزر با میله‌های واقعی صدا (`CircleBarVisualizer`)
- 📋 صفحهٔ لیست آهنگ پلی‌لیست + حذف تکی هر تراک

## v5.10.4
- 📥 دانلود آهنگ در Now Playing و پروکسی — وضعیت زنده + لغو
- 💜 ویژوالایزر پایدار — هنگام تعویض تراک حفظ می‌شود
- 📝 نسخهٔ تنظیمات از `BuildConfig` واقعی (نه رشتهٔ ثابت قدیمی)
- 🖼 فال‌بک کاور: آرتورک فایل موزیک ← عکس چت/کانال
- 🖥 پاریتی ویندوز (همین تغییرات در نسخهٔ ویندوز)

## v5.9.5 🔥
**فیکس‌های بحرانی:**
- 💥 کرش ProxyFragment (ping از thread → requireContext null) — کانتکست app-level
- 🔒 «اسکن در حال انجامه» دروغ: چک فالو‌ها پرچم جدا گرفت (`followChecking`) و دیگر اسکن دستی را قفل نمی‌کند
- 📚 **کتابخانه دائمی شد** (`library.json`): هر «افزودن به کتابخانه»/فالو-دیپ‌اسکن/آهنگ جدید فالو ذخیره می‌شود — بعد از ری‌استارت TRACKS و FAVORITES دیگر خالی نیستند
- 📜 صفحه دنبال‌شده‌ها داخل NestedScrollView — اسکرول کامل

## v5.9.4
- 🐛 فیکس مسیریابی: نتایج فالو→کتابخانه (TRACKS)، فقط آهنگ‌های جدید در FOLLOWED
- 📌 نوتیف اسکن پین‌شده (PRIORITY_MAX + ongoing)

## v5.9.3
- 💥 ضدکرش کامل bindSideMenu (NPE لاگ کاربر)
- 💜 ویژوالایزر نئونی بازنویسی از صفر (`NeonVisualizer`)
- 🔔 کارت‌های امن دنبال‌شده‌ها (حذف فقط با تأیید)
- 🔍 حذف دکمه سرچ از هدر + 📝 فیکس نسخه تنظیمات
- 🎁 دیالوگ «افزودن به کتابخانه؟» بعد از اسکن
- 🗑 دکمه «پاک کردن کتابخانه» در تنظیمات

## v5.9.2
- 🐛 وایر شدن واقعی UI انتخاب گروهی (ریشه: پچ ناقص)
- 💔 حذف گروهی از فیوریت

## v5.9.1
- 🔔 تب FOLLOWED کنار TRACKS (تب دوم)
- 📥 فالو = دیپ‌اسکن کامل خودکار
- 💜 فیکس ویژوالایزر (کاور ۶۴٪)
- 🔍 دکمه دیپ‌اسکن کانال‌ها

## v5.9.0
- 🔍 اسکن عمیق بی‌نهایت (بدون سقف ۳۰۰۰)
- ☑️ انتخاب گروهی آهنگ‌ها/دانلودها
- 🔔 فالو کردن چت‌ها/کانال‌ها + صفحه دنبال‌شده‌ها
- 📲 نوتیف اسکن (پیشرفت + پایان)
- 💜 ویژوالایزر نئونی + 💥 پالس کاور

## v5.9.x - v5.8.x (خلاصه)
- v5.8.3: کرش خاتمه نشست از راه دور (gate + توقف امن پخش)
- v5.8.2: کارت کلید API شخصی در تنظیمات (فیکس «کد نمی‌آید»)
- v5.8.1: فیکس کرش استارتاپ (sideProxy)
- v5.8.0: کاور دایره‌ای HD، سپر اتصال، سوییچ پروکسی، سرچ چت‌ها، حذف فایل واقعی

## v5.7.0
- 💾 فقط فیوریت/پلی‌لیست دائمی؛ بقیه با اسکن
- ✏️🗑⬇️ ادیت/حذف/دانلود پلی‌لیست
- 💬 تب چت‌ها + 🇮🇷 «آهنگ‌ها»

## v5.6.0
- 🎨 رنگ اکسنت واقعی (۶ تم کامل)
- 🌐 دوزبانه FA/EN کامل
- 🖼 لوگوی جدید شفاف

## v5.5.0
- 🎨 ری‌دیزاین مطابق اسکرین‌شات‌های استور
- 🖼 تامبنیل‌ها (کاور آلبوم→کانال)
- 🔍 فیکس سرچ + تب SCAN جدا + ویژوالایزر
